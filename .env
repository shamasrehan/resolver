# Server configuration
PORT=9001
NODE_ENV=development
# Default contract language (solidity, vyper, rust)
DEFAULT_LANGUAGE=solidity

# OpenAI model
DEFAULT_MODEL=gpt-4

# Optional: Database connection (for future implementation)
# DB_URI=mongodb://localhost:27017/smartcontracts